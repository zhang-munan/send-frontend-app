# name: Publish and deploy H5

# on:
#   release:
#     types: [published]
#   workflow_dispatch:
#     inputs:
#       version:
#         description: '已有 H5 Release 标签，例如 v1.0.0'
#         required: true
#         type: string

# permissions:
#   contents: read
#   packages: write

# env:
#   REGISTRY: ghcr.io

# jobs:
#   publish-and-deploy:
#     name: Publish image and deploy production
#     runs-on: ubuntu-latest
#     environment: production
#     steps:
#       - uses: actions/checkout@v4
#         with:
#           ref: ${{ github.event.release.tag_name || inputs.version }}
#           fetch-depth: 0
#       - name: Verify release branch provenance
#         run: |
#           git fetch origin '+refs/heads/release/*:refs/remotes/origin/release/*'
#           if ! git branch -r --contains HEAD | grep -qE 'origin/release/'; then
#             echo "H5 生产版本必须来自 release/ 分支。"
#             exit 1
#           fi
#       - name: Download and verify the HBuilderX artifact
#         env:
#           GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#           VERSION: ${{ github.event.release.tag_name || inputs.version }}
#         run: |
#           gh release download "$VERSION" --pattern h5-dist.tar.gz
#           mkdir h5-dist
#           tar -xzf h5-dist.tar.gz -C h5-dist
#           test -f h5-dist/index.html
#           echo "IMAGE=${REGISTRY}/${GITHUB_REPOSITORY,,}:${VERSION}" >> "$GITHUB_ENV"
#       - uses: docker/login-action@v3
#         with:
#           registry: ${{ env.REGISTRY }}
#           username: ${{ github.actor }}
#           password: ${{ secrets.GITHUB_TOKEN }}
#       - uses: docker/build-push-action@v6
#         with:
#           context: .
#           file: Dockerfile.h5
#           push: true
#           tags: ${{ env.IMAGE }}
#       - name: Update H5 on the server
#         env:
#           DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
#           DEPLOY_PORT: ${{ secrets.DEPLOY_PORT }}
#           DEPLOY_USER: ${{ secrets.DEPLOY_USER }}
#           DEPLOY_SSH_KEY: ${{ secrets.DEPLOY_SSH_KEY }}
#         run: |
#           install -m 700 -d ~/.ssh
#           printf '%s\n' "$DEPLOY_SSH_KEY" > ~/.ssh/deploy_key
#           chmod 600 ~/.ssh/deploy_key
#           ssh-keyscan -p "${DEPLOY_PORT:-22}" -H "$DEPLOY_HOST" >> ~/.ssh/known_hosts
#           ssh -i ~/.ssh/deploy_key -p "${DEPLOY_PORT:-22}" "$DEPLOY_USER@$DEPLOY_HOST" \
#             "cd /opt/apps/bangni-shuochukou && H5_IMAGE='$IMAGE' docker compose --env-file .env.production -f compose.yml pull h5 && H5_IMAGE='$IMAGE' docker compose --env-file .env.production -f compose.yml up -d --no-deps h5 && docker image prune -f"
